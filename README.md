# Autonomous Research Agent

A LangGraph-orchestrated agent that plans a multi-step research strategy,
calls tools (web search, code execution) in a loop, re-plans based on what
it finds, and produces a cited research report.

Work in progress — see commit history for build phases. Full README with
architecture diagram and setup instructions lands in the final phase.
